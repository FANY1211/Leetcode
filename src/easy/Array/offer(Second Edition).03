
class Solution:
    def findRepeatNumber(self, nums: List[int]) -> int:
        list = []
        n = len(nums) - 1
        for i in range(n):
            if nums[i] not in list:
                list.append(nums[i])
            else:
                print(nums[i])
                break
