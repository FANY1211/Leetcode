class Solution:
    def printNumbers(self, n: int) -> List[int]:
        list = []
        for i in range(1, 10 ** n):
            list.append(i)
        
        return list
   
以上，被吐槽脱裤子放屁写法
直接：return range(1,100**n)

貌似是考察大数写法。。。下次再搞
        
